The main python code and the databases (de421 file and the hip data file(unzipped)) should be in the same directory.
Also can plot Analemma type plots for various stars, planets.
Can be expanded to include other databases.
